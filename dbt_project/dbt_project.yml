# Real dbt_project.yml, run for real by actual dbt-core + dbt-duckdb via
# real_tools/run_dbt_real.py - see ../README.md. Nothing here is a
# stand-in syntax; this is genuine dbt project structure, actually
# executed by the real `dbt` CLI, not just described.

name: 'birth_registrations'
version: '1.0.0'
config-version: 2

profile: 'birth_registrations'

model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  birth_registrations:
    staging:
      +materialized: view
