{
  "name": "data-poc-dashboard",
  "private": true,
  "type": "module",
  "description": "Vitest coverage for dashboard/qa-reporting-dashboard.template.html's inline JS (Phase 6 step 5, plans/publishing-and-history.md) - the Python side (qa_tools/pipeline/generator) is tested separately via uv run pytest.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^30.1.0",
    "vitest": "^5.0.1"
  }
}
