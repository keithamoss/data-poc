"""Real dbt-core/Soda Core/datacontract-cli/Evidently pipeline runs, one
subpackage per dataset (bdm/, cp/) plus tool-generic shared code (common/)
- see plans/wider.md #20 for why this became a package rather than a flat
sys.path-hacked directory of scripts."""
